{
  "name": "jaree-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "local": "turbo local --parallel",
    "start": "turbo local --parallel",
    "server:local": "turbo local --filter=jaree-server-repo"
  },
  "packageManager": "pnpm@10.11.1",
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
